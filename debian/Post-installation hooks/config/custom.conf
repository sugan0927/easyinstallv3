#!/bin/bash
# EasyInstall Custom Configuration Overrides
# This file overrides settings from easyinstall.conf
# Create this file to preserve custom settings across updates

# Example customizations:
# ENABLE_THEME_EDITOR="false"        # Disable theme editor
# DISABLE_XMLRPC="true"               # Block XML-RPC by default
# PHP_MEMORY_LIMIT="512M"             # Override PHP memory limit
# ADMIN_EMAIL="admin@example.com"      # Set admin email
